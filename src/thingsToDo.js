/*
3) The Previous Attempts panel drags a bit jerkily it passes over the recall
area -- At least I made it less bad than before (when it was awful) by giving the
<FloatingPanelNEW> the function to deal with the dragging of the panel (rather
than getting its parent to deal with dragging). I los made <Table> and
<RecallCard> PureComponents.
*/
